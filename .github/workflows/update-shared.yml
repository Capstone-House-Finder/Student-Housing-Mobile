name: Update Shared Package
on:
  workflow_dispatch:
  schedule:
    - cron: '0 9 * * 1'
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: mobile-app
      - uses: actions/checkout@v4
        with:
          repository: Capstone-House-Finder/Student-Housing
          path: web-platform
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - name: Authenticate GitHub Packages
        run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.NODE_AUTH_TOKEN }}" > ~/.npmrc
      - run: npm update @capstone-house-finder/shf-api
      - run: npm ci && npx tsc --noEmit
      - uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "chore: update @capstone-house-finder/shf-api"
          title: "chore: update shared package"
          body: "Automated PR - updates @capstone-house-finder/shf-api. TypeScript check passed."
          branch: "chore/update-shared"
